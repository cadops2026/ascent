# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build
node_modules
dist
dist-ssr
*.local

# Environment / secrets — NEVER commit (invariant #10: no client-side secrets)
.env
.env.*
!.env.example

# Supabase local artifacts
supabase/.branches
supabase/.temp
supabase/.env

# Deno lockfile — generated by local `deno check`; functions deploy via the Supabase CLI
deno.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Personal financial documents — never commit (statements, confirms, photos)
Ascent DOCS/*
!Ascent DOCS/*.md
